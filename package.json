{
  "name": "visual-check",
  "private": true,
  "scripts": {
    "test": "pnpm --filter playwright test",
    "dev": "pnpm --filter dashboard dev",
    "lint": "pnpm -r exec eslint .",
    "install-browsers": "pnpm --filter playwright exec playwright install chromium"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^6.0.2",
    "@types/node": "^25.5.0"
  }
}
